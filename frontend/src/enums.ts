export const enum Events {
    OPEN_SNACKBAR = 'toggleSnackbar',
    CLOSE_SNACKBAR = 'closeSnackbar',
}

export const enum SnackbarColor {
    success = 'success',
    error = 'error',
    warning = 'warning',
    info = 'info',
}

export const enum DefaultPages {
    PROFILE = 'profile',
    SEARCH = 'search',
    UPDATES = 'updates',
    SETTINGS = 'settings',
    
}